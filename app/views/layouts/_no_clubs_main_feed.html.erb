<div class="home-no-clubs-message">
  <h1>Looks like haven't joined any clubs!</h1>
  <h3>Have a club leader add you to get started!</h3>
  <br />
  <div class="container">
    <div class="row">
      <h3>Or, check out these open clubs: </h3>
      <div class="column">
        <table class="table">
          <tbody>
            <% Club.where(closed: false).all.each do |club| %>
              <tr><%= render club %></tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>
